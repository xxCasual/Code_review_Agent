# Demo Input

Use this command to generate a static portfolio report without submitting a real PR review:

```bash
UV_PROJECT_ENVIRONMENT=review-agent uv run review-agent demo --output examples/demo-report.md
```
